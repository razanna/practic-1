#include <stdio.h>

int main() {
    int n;

    printf("Введите число: ");
    scanf("%d", &n);

    // Проверяем, что n > 0 и n & (n-1) == 0
    int result = (n > 0) && ((n & (n - 1)) == 0);

    printf("%d\n", result);

    return 0;
}
